/**
 * Color de la paleta de la cascada del SDR, en canales de 0 a 255.
 */
export interface WaterfallColor {
  /** Rojo. */
  readonly r: number;
  /** Verde. */
  readonly g: number;
  /** Azul. */
  readonly b: number;
}

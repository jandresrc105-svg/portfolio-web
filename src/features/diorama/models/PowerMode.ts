/**
 * Forma en que un elemento se enciende en la intro.
 */
export enum PowerMode {
  /** Encendido suave y progresivo (linternas, luz interior). */
  Fade = 'fade',
  /** Arranque de tubo fluorescente/neón: parpadea antes de quedar fijo. */
  Strike = 'strike',
}
